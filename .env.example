# Example env file (do not commit real secrets here)
# Set this in Vercel (Project Settings → Environment Variables) or in your local .env.local (ignored by git)
GEMINI_API_KEY=your_gemini_api_key_here
# Optional fallback name used by some code paths
API_KEY=
